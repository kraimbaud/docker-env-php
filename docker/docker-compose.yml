version: '3'

services:
    php-fpm:
        build: ./php
        container_name: php-fpm-melody
        links:
            - db
        ports:
            - 8000:8000
        working_dir: /var/www/html/
        volumes:
            - ../app:/var/www/html
        tty: true
        env_file:
            - ./docker.env
        entrypoint: /entrypoint.sh

    nginx:
        restart: always
        build: ./nginx
        container_name: nginx-melody
        links:
            - php-fpm
            - node
        ports:
            - 80:80
            - 443:443
        working_dir: /var/www/html
        volumes:
            - ./nginx/conf.d:/etc/nginx/conf.d
            - data:/etc/letsencrypt
            - data:/var/log/nginx

    db:
        image: mysql:8
        container_name: db-melody
        ports:
            - 3306:3306
        environment:
            - MYSQL_RANDOM_ROOT_PASSWORD=true
            #- MYSQL_ROOT_PASSWORD=root
            - MYSQL_DATABASE=melody
            - MYSQL_USER=admin
            - MYSQL_PASSWORD=admin
        volumes:
            - data:/var/lib/mysql

    phpmyadmin:
        image: phpmyadmin/phpmyadmin:4.7
        container_name: phpmyadmin-melody
        links:
            - db
        ports:
            - 8080:80

    node:
        build:
            context: ./node
            dockerfile: ./Dockerfile
        container_name: node-melody
        tty: true
        working_dir: /var/www
        volumes:
            - ../app:/var/www

volumes:
    data: {}
